<h1>Listing users</h1>

<table>
  <tr>
    <th>User</th>
    <th></th>
  </tr>
  
<% @profiles.each do |profile| %>
  <tr>
    <td><%= profile.avatar %></td>
    <td><%= link_to profile.user.username, profile %></td>
  </tr>
<% end %>
</table>

<br />
<div id="footer">
  <p><%= link_to 'Back to your dashboard', dashboards_path %> | <%= link_to 'SIGN OUT', destroy_user_session_path if current_user %></p>